/**
 * file: subband_utils.hpp
 * Provides some (mainly numerical) utitlity functions for the subband calculaitons
 * such as scattering rates, fermi-dirac distributions etc.
 *
 */
#ifndef _SUBBAND_UTILS_HPP_
#define _SUBBAND_UTILS_HPP_

//#include <cmath>
//#include <utils/constants.hpp>

/********** lineshape functions **********/
//
// all LS-functions are normalized so that they integrate to 1
//

// Lorenzian centered at E0 with FWHM dE evaluated at E
//#define LORENZ(E,E0,dE) dE/(2.0f*MC::pi)/(SQR(E-E0)+SQR(dE/2.))
// sinc^2
// #define SINC2(E,E0,dE) std::sin(
#endif


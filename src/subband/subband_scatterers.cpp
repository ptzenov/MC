#include <subband/subband.hpp>
#include <utils/constants.hpp>
#include <utils/numeric.hpp>

//double calc_LO_emission_rate(MC::SubbandState const& in,
			     //MC::SubbandState const& fin, double E_LO) {
	
	
	//// check if emission is possible
	//double tol = 1e-3 * MC::q0;
	//if (!abs(fin.E_tot() - init.E_tot() - E_LO) > tol)
		       //return 0.0;
	
	//double rate = 0.f;
	//return rate;
//}
//double calc_LO_absorption_rate(MC::SubbandState const& in,
			       //MC::SubbandState const& fin, double E_LO) {
	//double rate = 0.f;
	//return rate;
//}

//MC::LOphononScatterer::LOphononScatterer(
    //std::vector<MC::SubbandState> const& states, double E_LO)
    //: _E_LO(E_LO),
      //_rates_em{new double[(states.size() * states.size())]},
      //_rates_abs{new double[(states.size() * states.size())]} {
	//for (auto i = 0U; i < states.size(); ++i) {
		//for (auto j = 0U; j < states.size(); ++j) {
			//_rates_em[j + i * states.size()] =
			    //calc_LO_emission_rate(states[i], states[j], _E_LO);
			//_rates_abs[j + i * states.size()] =
			    //calc_LO_absorption_rate(states[i], states[j],
						    //_E_LO);
		//}
	//}
//}

//bool MC::LOphononScatterer::operator()(MC::SubbandState& init,
				       //MC::SubbandState& fin, unsigned int t) {
	//// typically

	//return false;
/*}*/
